{
  "name": "@nestlancer/queue",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "amqplib": "^0.10.0",
    "@types/amqplib": "^0.10.0",
    "@nestjs/core": "^10.0.0",
    "rxjs": "^7.8.1"
  },
  "scripts": {
    "test:integration": "jest --testPathPattern=tests/integration",
    "test": "jest",
    "test:unit": "jest --testPathPattern=tests/unit",
    "test:cov": "jest --coverage"
  }
}
