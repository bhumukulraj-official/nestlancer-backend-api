apiVersion: apps/v1
kind: Deployment
metadata:
  name: email-worker
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: email-worker
    app.kubernetes.io/component: email-worker
    app.kubernetes.io/part-of: nestlancer
    tier: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: email-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: email-worker
        app.kubernetes.io/component: email-worker
        app.kubernetes.io/part-of: nestlancer
        tier: worker
    spec:
      serviceAccountName: nestlancer-sa
      containers:
        - name: email-worker
          image: ghcr.io/nestlancer/nestlancer-email-worker:latest
          envFrom:
            - configMapRef:
                name: nestlancer-config
            - secretRef:
                name: nestlancer-secrets
          env:
            - name: SERVICE_NAME
              value: "email-worker"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      restartPolicy: Always
