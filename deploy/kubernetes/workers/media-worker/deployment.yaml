apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-worker
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: media-worker
    app.kubernetes.io/component: media-worker
    app.kubernetes.io/part-of: nestlancer
    tier: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: media-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: media-worker
        app.kubernetes.io/component: media-worker
        app.kubernetes.io/part-of: nestlancer
        tier: worker
    spec:
      serviceAccountName: nestlancer-sa
      containers:
        - name: media-worker
          image: ghcr.io/nestlancer/nestlancer-media-worker:latest
          envFrom:
            - configMapRef:
                name: nestlancer-config
            - secretRef:
                name: nestlancer-secrets
          env:
            - name: SERVICE_NAME
              value: "media-worker"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      restartPolicy: Always
