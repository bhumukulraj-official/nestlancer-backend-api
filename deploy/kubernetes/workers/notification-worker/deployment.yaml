apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-worker
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: notification-worker
    app.kubernetes.io/component: notification-worker
    app.kubernetes.io/part-of: nestlancer
    tier: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: notification-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: notification-worker
        app.kubernetes.io/component: notification-worker
        app.kubernetes.io/part-of: nestlancer
        tier: worker
    spec:
      serviceAccountName: nestlancer-sa
      containers:
        - name: notification-worker
          image: ghcr.io/nestlancer/nestlancer-notification-worker:latest
          envFrom:
            - configMapRef:
                name: nestlancer-config
            - secretRef:
                name: nestlancer-secrets
          env:
            - name: SERVICE_NAME
              value: "notification-worker"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      restartPolicy: Always
