apiVersion: v1
kind: Service
metadata:
  name: users
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: users
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3012
      targetPort: 3012
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: users
