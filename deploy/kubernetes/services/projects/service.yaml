apiVersion: v1
kind: Service
metadata:
  name: projects
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: projects
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3015
      targetPort: 3015
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: projects
