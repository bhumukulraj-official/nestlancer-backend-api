apiVersion: v1
kind: Service
metadata:
  name: progress
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: progress
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3016
      targetPort: 3016
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: progress
