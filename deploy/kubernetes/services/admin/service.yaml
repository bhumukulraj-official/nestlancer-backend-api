apiVersion: v1
kind: Service
metadata:
  name: admin
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: admin
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3024
      targetPort: 3024
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: admin
