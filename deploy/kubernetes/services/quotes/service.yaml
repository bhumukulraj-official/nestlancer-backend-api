apiVersion: v1
kind: Service
metadata:
  name: quotes
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: quotes
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3014
      targetPort: 3014
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: quotes
