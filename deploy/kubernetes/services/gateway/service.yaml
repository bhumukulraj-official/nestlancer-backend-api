apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: gateway
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: gateway
