apiVersion: v1
kind: Service
metadata:
  name: notifications
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: notifications
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3019
      targetPort: 3019
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: notifications
