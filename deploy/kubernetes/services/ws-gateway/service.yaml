apiVersion: v1
kind: Service
metadata:
  name: ws-gateway
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: ws-gateway
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: ws-gateway
