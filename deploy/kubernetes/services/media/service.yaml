apiVersion: v1
kind: Service
metadata:
  name: media
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: media
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3020
      targetPort: 3020
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: media
