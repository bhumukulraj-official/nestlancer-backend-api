apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: auth
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3011
      targetPort: 3011
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: auth
