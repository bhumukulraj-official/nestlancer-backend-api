apiVersion: v1
kind: Service
metadata:
  name: blog
  namespace: nestlancer
  labels:
    app.kubernetes.io/name: blog
    app.kubernetes.io/part-of: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 3022
      targetPort: 3022
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: blog
