apiVersion: v1
kind: Service
metadata:
  name: redis-pubsub
  namespace: nestlancer
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app.kubernetes.io/name: redis-pubsub
