apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: nestlancer
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: gateway
        metrics_path: /api/v1/metrics
        static_configs:
          - targets: ['gateway:3000']
